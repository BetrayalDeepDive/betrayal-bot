name: Shorts Schedule [DISABLED]

on:
  # DISABLED — Shorts now auto-generated inside empire_v3_automation.yml
  # stage4_upload.py creates 2 Shorts per video automatically
  # - cron: '30 6 * * *'
  # - cron: '30 8 * * *'
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "shorts_schedule.yml is DISABLED."
          echo "YouTube Shorts are now auto-created inside empire_v3 pipeline."
          echo "Short 1 (teaser): from 10% into main video"
          echo "Short 2 (recap): from 67% into main video"
          echo "Both uploaded automatically after main video approval."
          exit 0
