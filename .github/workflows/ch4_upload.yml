# ============================================================
# THE ARCHIVE — Ch4 Upload Workflow
# Phase 2: UPLOAD only (reads from pending_upload.json in repo)
# Runs: Tue-Sat 9:00 PM IST = 15:30 UTC
# 1h before Ch1 upload — stagger uploads across the evening
# US: 11:30 AM ET | UK: 4:30 PM BST — US afternoon indexing
# ============================================================
name: Ch4 The Archive — Upload

on:
  schedule:
    - cron: '30 15 * * 2-6'   # 9:00 PM IST | Tue-Sat
  workflow_dispatch:

env:
  PYTHONUNBUFFERED: "1"
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"
  PHASE: "upload"

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pillow -q

      - name: Pull latest state
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Actions"
          git pull --rebase origin main || true

      # ── FIX: pull back actual video/audio/thumbnail files from Generate ──
      - name: Download video artifact from latest Generate run
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: ch4_generate.yml
          name: ch4-pending-video
          path: /home/runner/work/archive
          workflow_conclusion: success
          if_no_artifact_found: fail

      - name: Upload pre-generated video
        env:
          ARCHIVE_YT_CLIENT_ID:      ${{ secrets.ARCHIVE_YT_CLIENT_ID }}
          ARCHIVE_YT_CLIENT_SECRET:  ${{ secrets.ARCHIVE_YT_CLIENT_SECRET }}
          ARCHIVE_YT_REFRESH_TOKEN:  ${{ secrets.ARCHIVE_YT_REFRESH_TOKEN }}
          TELEGRAM_TOKEN:            ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID:          ${{ secrets.TELEGRAM_CHAT_ID }}
          PIPELINE_PHASE:            "upload"
        run: |
          export PYTHONPATH="${PYTHONPATH}:$(pwd)/channels"
          python -u channels/archive/archive_pipeline.py

      - name: Commit upload state
        if: always()
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Actions"
          git add channels/archive/archive_state.json \
                  channels/archive/pending_upload.json || true
          git diff --staged --quiet || git commit -m "chore: ch4 uploaded [skip ci]"
          git push origin main || true
