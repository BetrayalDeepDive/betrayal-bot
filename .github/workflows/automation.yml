name: Automation [DISABLED]

on:
  # DISABLED — superseded by empire_v3_automation.yml
  # - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Old automation.yml is DISABLED."
          echo "All automation now runs through empire_v3_automation.yml"
          exit 0
