name: Video Automation [DISABLED]

on:
  # DISABLED — replaced by 4-stage empire_v3_automation.yml
  # - cron: '0 2 * * 1,3,5'
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "video_automation.yml is DISABLED."
          echo "Video production now runs through empire_v3_automation.yml"
          echo "4 stages: generate-script, generate-audio, assemble-video, upload-with-approval"
          exit 0
