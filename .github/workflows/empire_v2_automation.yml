name: Empire V2 - Full Automation [DISABLED]

on:
  # DISABLED — replaced by empire_v3_automation.yml
  # - cron: '0 2 * * 1,3,5'
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Empire V2 is DISABLED."
          echo "All video production now runs through empire_v3_automation.yml"
          exit 0
